# SQLite. Versions 3.8.0 and up are supported.
#   gem install sqlite3
#
#   Ensure the SQLite 3 gem is defined in your Gemfile
#   gem 'sqlite3'
#
default: &default
  adapter: sqlite3
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  timeout: 5000

development:
  <<: *default
  database: db/development.sqlite3
  # Maybe prevent db locking while Sidekiq is running
  timeout: 30000

<<<<<<< HEAD
=======
publicdev:
  <<: *default
  #database: db/pub/development.sqlite3
  # Maybe prevent db locking while Sidekiq is running
  #timeout: 20000
  adapter: mysql2
  database: "<%= ENV.fetch('MYSQL_DATABASE_NAME') %>"
  username: "<%= ENV.fetch('MYSQL_DATABASE_USER') %>"
  password: "<%= ENV.fetch('MYSQL_DATABASE_PASS') %>"
  host:     "<%= ENV.fetch('MYSQL_DATABASE_HOST') %>"
>>>>>>> b95cbb2 (quote env vars in database.yml)

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  <<: *default
  database: db/test.sqlite3

production:
  <<: *default
  database: db/production.sqlite3
