<%#
  # Blacklight will force the title field in here, but overrides in
  # helpers/umedia/localizable_helper.rb allow us to do more at runtime
  # than is configured at compile time in CatalogController
%>
<%= render_localized_document_heading %>
